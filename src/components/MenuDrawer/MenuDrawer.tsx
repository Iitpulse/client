import styles from "./MenuDrawer.module.scss"

const MenuDrawer = () => {
    return (
        <div className={styles.container}>
            This is Menu Drawer
        </div>
    )
}

export default MenuDrawer
